sudo: required
services:
    - docker

before_install:
    - docker build -t jainankur2/docker-react -f Dockerfile2.dev .

script:
    - docker run -e CI=true jainankur2/docker-react npm run test -- --coverage
language: generic
